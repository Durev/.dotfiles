set icons
set previewer ~/.config/lf/previewer.sh

# Open finder
cmd finder %open .
map o finder

# Trash
cmd trash %set -f; mv $fx ~/.Trash
map <backspace2> trash

# Show hidden files
map H set hidden!
